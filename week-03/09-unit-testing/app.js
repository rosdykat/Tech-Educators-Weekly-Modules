// Export → file to export
// import → where the file will be exported to

export function sum(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}

// I will be running my unit tests in a seperate file called fileName.test.js
